/**
 * Water-FX library barrel.
 *
 * Declarative, prop-driven, purely presentational effects for the feed. Each
 * component's props are the matching interface in `@/feed/contracts`. All honour
 * `prefers-reduced-motion` and use transform/opacity-only motion.
 */
export { LikeSplash } from "./LikeSplash";
export { DepthGauge } from "./DepthGauge";
export { SoundDisc } from "./SoundDisc";
export { TideBuffer } from "./TideBuffer";
export { PullWave } from "./PullWave";
