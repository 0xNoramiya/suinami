# Hackathon-friendly: don't let transitive peer mismatches fail the install.
strict-peer-dependencies=false
auto-install-peers=true
# Keep node-gyp / prebuild output reproducible across the team.
node-linker=isolated
